# 專案功能調整與後續開發備忘錄

本檔案記錄了暫時停用的重要功能，以及未來重新啟用、修改時的具體設計規範。

---

## 📌 暫時停用的自訂呈現元素

為了提供使用者最乾淨的「純閱讀」體驗，以下兩項有關學術比對的功能已被暫時隱藏與關閉：

1. **顯示校勘標記與連結**（點選段落內 `[1]` 等角標可開啟下方校勘註解面板）
2. **顯示大藏經原始頁碼**（例如每段行頭前顯示 `p0778a` 等原始大正藏頁行號標籤）

---

## 🛠️ 後續開啟與修正指引

當未來需要重新啟用並對這兩項功能進行修改時，請參考以下程式碼位置與重啟步驟：

### 1. 設定面板（SettingsView）

* **檔案路徑**：[SettingsView.tsx](file:///C:/Users/vbgrd/OneDrive/桌面/Cbeta%20Reader/src/reader/components/SettingsView.tsx)
* **重啟位置**：約在 `自訂呈現元素` 的區塊（約 Line 110 ~ Line 128）。
* **修改做法**：移除被包覆的註解 `/* ... */`，即可在「其他閱讀設定」中重新顯現「顯示校勘標記與連結」和「顯示大藏經原始頁碼」的勾選開關。

### 2. 閱讀面板經文渲染（ReaderView）

* **檔案路徑**：[ReaderView.tsx](file:///C:/Users/vbgrd/OneDrive/桌面/Cbeta%20Reader/src/reader/components/ReaderView.tsx)
* **重啟位置**：在渲染 `sutraSegments.map` 迴圈段落的部分（約 Line 483 ~ Line 510）。
* **修改做法**：
  * **原始頁碼**：將 `{false && settings.customVisibleElements.pageNumber && (` 的 `false &&` 移除。
  * **校勘標記**：將 `{false && settings.customVisibleElements.notes && ...` 的 `false &&` 移除，即可恢復段落行間的頁碼與角標渲染。
  * **點擊觸發與側邊欄**：
    * 尋找 `handleSegmentClick` 函式（約 Line 320），將其中被註解的 `if (seg.notes ...)` 段落點擊彈出邏輯取消註解。
    * 尋找最底部的校勘側邊欄渲染（約 Line 670），將 `{false && selectedNotes && (` 的 `false &&` 移除，即可完全重啟「校勘註解對照」面板。
